<view class="nav_container">
  <view wx:for="{{categories}}" class="nav_item">
    <image class="item-image" src="{{item.small_pic}}" class="small_pic" />
    <text>{{item.name}}</text>
  </view>
</view>
