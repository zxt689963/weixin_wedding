<view class="recent_wrapper">
    <view wx:for="{{recent}}" class="recent_item">
        <image data-id="{{item.id}}" bind:tap="onTap" class="item-image" src="{{item.big_pic}}" class="big_pic" />
    </view>
</view>