<block wx:for="{{categories}}">
    <view>
        <image class="categories_img" data-id="{{item.id}}" bind:tap="onTap" src="{{item.big_pic}}" />
        <view>{{item.name}}</view>
        <view>{{item.price}}</view>
    </view>
</block>

<v-support />